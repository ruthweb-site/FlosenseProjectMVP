/**
 * firebase-config.js
 * 
 * NOTE: This file is now served dynamically by the Express server (server.js)
 * to inject environment variables at runtime and keep secrets out of source control.
 * 
 * If you see this content in the browser, something might be wrong with the route handling.
 */
